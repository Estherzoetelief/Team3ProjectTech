<nav class="navbar">
    <div class="navLeft">
        <a href="/discover" class="active">Discover</a>
        <a href="/find-requests">Jobs</a>
    </div>
    <div class="navCenter">
        <a href="#">Spark</a>
    </div>
    <div class="navRight">
        <a href="#" class="heart">
            <img src="img/heart.png">
        </a>
        <a href="#" class="lightning">
            <img src="img/lightning.png">
        </a>
        <% if (session.user) { %>
          <a href="#" class="profile">
              <img src="upload/<%= profile_picture %>">
              <p><%= session.user.username %></p>
          </a>
        <% } else { %>
          <a href="/sign-in">Log in</a>
        <% } %>
    </div>
</nav>
